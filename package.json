{
  "name": "motionmag",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser-only Eulerian Video Magnification — see your pulse in your face. No backend.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
